package com.headlamp;
/**
 * HDLmStartupMode short summary.
 *
 * HDLmStartupMode description.
 *
 * @version 1.0
 * @author Peter
 */ 
/* The enum below provided values showing if startup mode
   is active or not. Startup mode is only set to yes, if 
   we are in startup mode. Otherwise, startup mode is set
   to no. */ 
public enum HDLmStartupMode {
	NONE,
	STARTUPMODENO,
	STARTUPMODEYES
}