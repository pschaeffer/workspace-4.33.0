package com.headlamp;
/**
 * HDLmLogMatchingTypes short summary.
 *
 * HDLmLogMatchingTypes description.
 *
 * @version 1.0
 * @author Peter
 */
/* The enum below provides values showing if modification matching 
   should be logged or not. If this enum is set to yes or true (see
   the actual values), then the modification matching process is
   logged in some detail. If this enum is set to no or false (see
   the actual values), then modification matching is not logged. */
public enum HDLmLogMatchingTypes {
	NONE,
	LOGMATCHINGYES,
	LOGMATCHINGNO
}